{
  "name": "mqtt-test",
  "version": "0.0.1",
  "description": "Lambda InvokeAsync to MQTT test",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "",
  "dependencies": {
    "mqtt": "^0.3.13"
  }
}
